import React from 'react';
import s from './style.module.css'

const OrderButton = () => {
    return (
        <button className={s.ord_btn}>
            Order Today
        </button>
    );
};

export default OrderButton;